---
# Install these requirements using:
# ansible-galaxy install --roles-path ./external-roles -r ./requirements/roles.yml
- name: acme
  src: https://github.com/NIXKnight/Ansible-Acme.git
  version: main
